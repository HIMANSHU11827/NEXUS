# Skill Forge
**Version:** 1.0.0 — auto-bumped via `VersionManager` on refine.

Creates and refines procedural skill memories in `skills/` directory. Each skill gets a `SKILL.md` with version metadata.
