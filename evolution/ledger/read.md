# Ledger

Event auditing and logging subsystem. Records all system actions, agent decisions, and state transitions.

## Key Classes
- `Ledger` — Event recording engine
- Methods: `record()`, `query()`, `replay()`
