# Forge (ToolForge)
**Version:** 1.0.0 — auto-bumped via `VersionManager` on refine.

Creates new NEXUS tools under `tools/<name>/` with schema, implementation script, and documentation. Supports refinement with version bumping.
