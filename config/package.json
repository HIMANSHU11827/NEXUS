{
  "name": "nexus-sovereign",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "cd cli && npm start",
    "api": "python -m server",
    "ui": "npm --prefix gui run dev"
  },
  "devDependencies": {
    "jest": "^30.4.2"
  }
}
