# Test Evolution Nudge

Tests for the `NudgeEngine` module (`evolution.nudge.engine`), covering:

- **test_init**: Verifies that a `NudgeEngine` instance is initialized with the correct root path.
- **test_state_path**: Ensures the `state_path` attribute is set after initialization.
