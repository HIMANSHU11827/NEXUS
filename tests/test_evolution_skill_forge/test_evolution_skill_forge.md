# Test Evolution Skill Forge

Tests for the `SkillForge` module (`evolution.skill_forge.forge`), covering:

- **test_init**: Verifies that a `SkillForge` instance is initialized with the correct root path.
- **test_forge_skill**: Ensures `forge.forge()` accepts a skill name and description and returns a dictionary result.
