# Test Evolution Knowledge Forge

Tests for the `KnowledgeForge` module (`evolution.knowledge_forge.forge`), covering:

- **test_init**: Verifies that a `KnowledgeForge` instance is initialized with the correct root path.
- **test_lib_dir**: Ensures the `lib_dir` attribute is set after initialization.
