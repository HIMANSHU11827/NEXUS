# Test Main
**Version:** 2.0.0

Core system tests — NexusMoERouter streaming fallback, provider factory credential resolution, provider router streaming, shell TUI rendering, and tool security boundary enforcement.
