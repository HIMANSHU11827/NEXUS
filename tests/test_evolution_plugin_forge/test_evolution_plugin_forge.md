# Test Evolution Plugin Forge
**Version:** 1.0.0 — auto-bumped via `VersionManager` on refine.

Tests for the `PluginForge` module (`evolution.plugin_forge.forge`), covering:

- **test_init**: Verifies that a `PluginForge` instance is initialized with the correct root path.
- **test_forge_plugin**: Ensures `forge.forge()` accepts a plugin name and description and returns a dictionary result.
