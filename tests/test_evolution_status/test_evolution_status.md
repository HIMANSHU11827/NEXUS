# Test Evolution Status

Tests for the `EvolutionStatus` module (`evolution.status`), covering:

- **test_init**: Verifies that an `EvolutionStatus` instance is initialized with the correct root path.
- **test_report**: Ensures `status.report()` returns a dictionary containing a `"skills"` key.
