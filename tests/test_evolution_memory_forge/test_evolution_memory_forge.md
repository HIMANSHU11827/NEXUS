# Test Evolution Memory Forge
**Version:** 1.0.0 — auto-bumped via `VersionManager` on refine.

Tests for the `MemoryForge` module (`evolution.memory_forge.forge`), covering:

- **test_init**: Verifies that a `MemoryForge` instance is initialized with the correct root path.
- **test_memory_dir**: Ensures the `memory_dir` attribute is set after initialization.
