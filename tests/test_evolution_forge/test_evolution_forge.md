# Test Evolution Forge
**Version:** 1.0.0 — auto-bumped via `VersionManager` on refine.

Tests for the `ToolForge` module (`evolution.forge.engine`), covering:

- **test_init**: Verifies that a `ToolForge` instance is initialized with the correct root path.
- **test_forge_tool**: Ensures `forge.forge()` accepts a tool definition dictionary and returns a dictionary result.
