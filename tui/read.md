# TUI (Ink)

TypeScript Ink thin client that connects to the FastAPI GUI server on :8000.

**Version:** 2.0.0

## Usage
`powershell
cd tui
npm install
npm start
`

**Note:** This is NOT the live terminal — it is an Ink UI over the API. Use python -m nexus for the in-process terminal.
