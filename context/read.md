# Context

Context persistence and compression for session management.

**Version:** 1.0.0

## Components
- persistence.py — Session context save/load/resume
- NexusContextCompressor — LLM-driven high-fidelity history compaction
- Zero-token context engine (ID pointers + summary)
