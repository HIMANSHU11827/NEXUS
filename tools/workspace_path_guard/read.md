# workspace_path_guard

Sandbox currently blocks paths outside workspace but no explicit guard or validation exists to prevent workspace-escape attempts; NEXUS should enforce and log such violations.

## Usage

Describe how to use this tool.
