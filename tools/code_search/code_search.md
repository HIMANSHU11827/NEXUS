# Code Search Tool
**Version:** 1.0.0 — auto-bumped via `VersionManager` on refine.

Search codebase using regex (grep) or glob patterns.

## Parameters
- `pattern` (string, required): Search pattern
- `path` (string, optional, default=.): Search root
- `include` (string, optional): File filter (e.g. *.py)
- `mode` (string, optional, default=grep): grep | glob | struct
